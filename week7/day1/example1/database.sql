Create DATABASE gamerdatabase;

-- Create Table todo(
--   todoid Serial Primary Key, -- Make this Id unique from any otheer Ids
--   descripton VARCHAR(255)
-- )

-- Create Table gamer(
--   gamer_id Serial Primary Key,
--   userid Serial,
--   username VARCHAR(20)
--   plaform
-- )

-- Create Table platform(
--   platform_id Serial Primary Key,
--   platformName VARCHAR(20)
--   gamerPlatform
-- )
